# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets & local credentials — NEVER commit
.env
*.key
google_oauth_client.json
google_token.json

# Runtime logs and per-agent local state
*.log
agents/job_scraper/data/

# Web control-center local state (SQLite DB + prefs overlay)
data/

# OS / editor cruft
.DS_Store
.vscode/
.idea/
*.swp
*~

# Playwright MCP screenshot dumps
.playwright-mcp/

# Superpowers brainstorming mockups (visual companion scratch)
.superpowers/

# Next.js app (web-next) build + deps
web-next/node_modules/
web-next/.next/
web-next/.env

# Local dev tools (Tectonic LaTeX binary) — not committed
/tools/
